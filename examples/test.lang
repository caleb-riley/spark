func make_adder(amount: float): (float) -> float {
    func add(number: float): float {
        return number + amount;
    }

    return add;
}

var add_five = make_adder(5);
print(add_five(4));